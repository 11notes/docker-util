${{ content_synopsis }} This image provides you with eleven lib. A collection of simple yet useful tools to manage applications inside containers during build and during runtime. The images comes in two flavours:

- Only scripts
- Scripts and binaries

With usefull functions for entrypoints.

${{ content_source }}